{% extends 'admin/base.html.twig' %}

{% block title %}Nouvelle Question FAQ{% endblock %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1 class="h3">Nouvelle Question FAQ</h1>
                <a href="{{ path('admin_faq_index') }}" class="btn btn-secondary">
                    <i class="bi bi-arrow-left"></i> Retour
                </a>
            </div>

            <form method="post" class="needs-validation" novalidate>
                <div class="row">
                    <!-- FAQ Configuration -->
                    <div class="col-md-4">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Configuration de la FAQ</h5>
                            </div>
                            <div class="card-body">
                                <div class="mb-3">
                                    <label for="category" class="form-label">Catégorie</label>
                                    <input type="text" class="form-control" id="category" name="category" 
                                           value="{{ faq.category ?? '' }}" 
                                           placeholder="Général, Support, Facturation...">
                                    <div class="form-text">Permet de regrouper les questions</div>
                                </div>

                                <div class="mb-3">
                                    <label for="slug" class="form-label">Slug (URL)</label>
                                    <input type="text" class="form-control" id="slug" name="slug" 
                                           value="{{ faq.slug ?? '' }}" 
                                           placeholder="Sera généré automatiquement">
                                    <div class="form-text">Laissez vide pour génération automatique</div>
                                </div>

                                <div class="mb-3">
                                    <label for="sortOrder" class="form-label">Ordre de tri</label>
                                    <input type="number" class="form-control" id="sortOrder" name="sortOrder" 
                                           value="{{ faq.sortOrder }}" min="0">
                                </div>

                                <div class="mb-3">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" id="isActive" 
                                               name="isActive" value="1" 
                                               {{ faq.isActive ? 'checked' : '' }}>
                                        <label class="form-check-label" for="isActive">
                                            Question active
                                        </label>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Translations -->
                    <div class="col-md-8">
                        <div class="card">
                            <div class="card-header">
                                <h5 class="mb-0">Traductions</h5>
                            </div>
                            <div class="card-body">
                                <!-- Language Tabs -->
                                <ul class="nav nav-tabs" id="languageTabs" role="tablist">
                                    {% for language in languages %}
                                        <li class="nav-item" role="presentation">
                                            <button class="nav-link {{ loop.first ? 'active' : '' }}" 
                                                    id="{{ language.code }}-tab" 
                                                    data-bs-toggle="tab" 
                                                    data-bs-target="#{{ language.code }}-content" 
                                                    type="button" role="tab">
                                                {{ language.nativeName }}
                                                {% if language.isDefault %}
                                                    <span class="badge bg-primary ms-1">Défaut</span>
                                                {% endif %}
                                            </button>
                                        </li>
                                    {% endfor %}
                                </ul>

                                <!-- Language Content -->
                                <div class="tab-content" id="languageTabsContent">
                                    {% for language in languages %}
                                        <div class="tab-pane fade {{ loop.first ? 'show active' : '' }}" 
                                             id="{{ language.code }}-content" 
                                             role="tabpanel">
                                            <div class="p-3">
                                                <div class="mb-3">
                                                    <label for="question_{{ language.code }}" class="form-label">
                                                        Question *
                                                    </label>
                                                    <input type="text" 
                                                           class="form-control" 
                                                           id="question_{{ language.code }}" 
                                                           name="translations[{{ language.code }}][question]" 
                                                           required>
                                                </div>

                                                <div class="mb-3">
                                                    <label for="answer_{{ language.code }}" class="form-label">
                                                        Réponse *
                                                    </label>
                                                    <textarea class="form-control" 
                                                              id="answer_{{ language.code }}" 
                                                              name="translations[{{ language.code }}][answer]" 
                                                              rows="8" required></textarea>
                                                    <div class="form-text">La réponse complète à la question</div>
                                                </div>
                                            </div>
                                        </div>
                                    {% endfor %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row mt-4">
                    <div class="col-12">
                        <div class="d-flex justify-content-end gap-2">
                            <a href="{{ path('admin_faq_index') }}" class="btn btn-secondary">Annuler</a>
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-floppy"></i> Créer la question FAQ
                            </button>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
// Auto-generate slug from default language question
const defaultQuestionInput = document.getElementById('question_{{ defaultLanguage.code }}');
const slugInput = document.getElementById('slug');

if (defaultQuestionInput && slugInput) {
    defaultQuestionInput.addEventListener('input', function() {
        if (!slugInput.value) {
            const slug = this.value
                .toLowerCase()
                .replace(/[^\w\s-]/g, '')
                .replace(/[\s_-]+/g, '-')
                .replace(/^-+|-+$/g, '');
            slugInput.value = slug;
        }
    });
}
</script>
{% endblock %}